<Window x:Class="MunicipalityApp.ReportIssueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Report an Issue" 
        Height="720" 
        Width="860"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <!-- Outer Layout -->
    <Grid>
        <!-- Elegant gradient background -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#004D40" Offset="0.0"/>   <!-- Deep Teal -->
                <GradientStop Color="#00695C" Offset="1.0"/>   <!-- Soft Modern Teal -->
            </LinearGradientBrush>
        </Grid.Background>

        <!-- Main Card -->
    <Border CornerRadius="18"
        Background="White"
        Padding="40"
        Width="820"
        MinWidth="720"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Effect="{DynamicResource DropShadow}">
            
            <StackPanel>
                <!-- Header -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Image Source="/SA-coat-of-arms.png" Width="70" Height="45" Margin="0,0,12,0"/>
                    <TextBlock Text="Report an Issue"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryDarkTealBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Progress -->
                <ProgressBar x:Name="progressBar"
                             Height="10"
                             Minimum="0"
                             Maximum="100"
                             Value="0"
                             Margin="0,0,0,25"
                             Foreground="{StaticResource AccentYellowBrush}"
                             Background="#1A000000"
                             BorderThickness="0"/>

                <!-- Title -->
                <TextBlock Text="Title"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Margin="0,10,0,5"
                           Foreground="{StaticResource PrimaryDarkTealBrush}"/>
                <TextBox x:Name="txtTitle"
                         Height="42"
                         Margin="0,0,0,15"
                         Padding="10,5"
                         FontSize="14"
                         Style="{StaticResource TextBoxStyle}"
                         Tag="Enter issue title"/>

                <!-- Category -->
                <TextBlock Text="Category"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Margin="0,5,0,5"
                           Foreground="{StaticResource PrimaryDarkTealBrush}"/>
                <ComboBox x:Name="cmbCategory"
                          Height="42"
                          Margin="0,0,0,15"
                          Padding="10,5"
                          FontSize="14"
                          Style="{StaticResource ComboBoxStyle}">
                    <ComboBoxItem Content="Electricity"/>
                    <ComboBoxItem Content="Water"/>
                    <ComboBoxItem Content="Roads"/>
                    <ComboBoxItem Content="Waste"/>
                </ComboBox>

                <!-- Description -->
                <TextBlock Text="Description"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Margin="0,5,0,5"
                           Foreground="{StaticResource PrimaryDarkTealBrush}"/>
                <TextBox x:Name="txtDescription"
                         AcceptsReturn="True"
                         Height="90"
                         TextWrapping="Wrap"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,0,20"
                         Style="{StaticResource TextBoxStyle}"
                         Tag="Enter detailed description"/>

                <!-- File Attach -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                    <Button Content="Attach Media"
                            Style="{StaticResource AccentButton}"
                            Width="140"
                            Height="36"
                            Click="btnAttachMedia_Click"/>
                    <TextBlock x:Name="lblFilePath"
                               Text="No file selected."
                               Margin="12,8,0,0"
                               FontStyle="Italic"
                               Foreground="{StaticResource NeutralFaintBrush}"/>
                </StackPanel>

                <Separator Margin="0,10,0,20" Opacity="0.2"/>

                <!-- Submit Button -->
                <Button Content="Submit Report"
                        Style="{StaticResource PrimaryButton}"
                        Height="42"
                        FontSize="16"
                        Margin="0,0,0,15"
                        Click="btnSubmit_Click"/>

                <!-- Back Button -->
                <Button Content="Back to Main Menu"
                        Background="Transparent"
                        BorderBrush="{StaticResource PrimaryDarkTealBrush}"
                        Foreground="{StaticResource PrimaryDarkTealBrush}"
                        FontSize="14"
                        Height="40"
                        Click="btnBack_Click">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Color="#22000000"/>
                    </Button.Effect>
                </Button>
            </StackPanel>
        </Border>

        <!-- Using shared DropShadow from App.xaml -->

        <!-- Fade-in animation -->
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Grid.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1"
                                         Duration="0:0:0.8"
                                         AccelerationRatio="0.4"
                                         DecelerationRatio="0.6"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
    </Grid>
</Window>
